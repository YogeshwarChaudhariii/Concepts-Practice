#include<iostream>
#include<vector>
#include<algorithm>      // sort
using namespace std;

int main()
{
    vector<int> Arr = {5, 2, 4, 1, 3};

    sort(Arr.begin(), Arr.end());

    for (int i = 0; i < Arr[i]; i++)
    {
        cout<<Arr[i]<<" ";
    }
    

    return 0;
}
